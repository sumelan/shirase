# to convert any SSH Ed25519 public key to the age format,
# run `nix-shell -p ssh-to-age --run 'ssh-keyscan example.com | ssh-to-age'`
keys:
  - &sumelan_sakura age1jfy4xctrua3nttmxdej5537wu3aq24xf2yqpymuvkwe8sds2yqzqx7pcpc
  - &github_sakura age1z9rt0gyy0ptndrgz2jtt66rqqd7y3tp7evcvygzuf9ur4dtuddkqz08jwt
# Note: Be sure to not include a - before subsequent key types under key_groups
creation_rules:
  - path_regex: secrets/[^/]+\.(yaml|json|env|ini)$
    key_groups:
    - age:
      - *sumelan_sakura
      - *github_sakura

